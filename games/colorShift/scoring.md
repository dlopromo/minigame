# 轉色牌 Scoring

Room leaderboard result:

- Winner gets `+10`.
- Other players get negative remaining hand size.
- Winner receives `win: true`.

Future variants can replace this with official-style card-value scoring, but MVP
keeps it simple for room leaderboard ranking.
